Scratch host for review screenshots referenced from PR/MR comments.

Nothing here is product code: the images are rendered evidence for an open review
round, referenced by raw URL from the review comment. Frames are grouped by the
PR they belong to (`pr254/r2/`). Safe to delete once a round closes.
